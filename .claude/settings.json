{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/hardikkaushik/Downloads/Scraper && FILE=$(jq -r '.tool_input.file_path // empty') && [ -n \"$FILE\" ] && git add \"$FILE\" && git diff --cached --quiet || git commit -m \"fix: update $(basename $FILE)\" && git pull --rebase origin main && git push origin main 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
