services:
  - type: web
    name: CI-scraper
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.assistant_backend:app --timeout 120
    envVars:
      - key: GROQ_API_KEY
        sync: false
