services:
  - type: web
    name: actian-intelligence-assistant
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn assistant_backend:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
