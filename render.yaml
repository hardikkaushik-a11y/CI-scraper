services:
  - type: web
    name: actian-intelligence-assistant
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.assistant_backend:app
    envVars:
      - key: GROQ_API_KEY
        sync: false
